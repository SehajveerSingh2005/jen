# Jen sidecar (agent + STT + TTS) — Python dependencies
# Install with: pip install -r requirements.txt

# Audio & wake word
numpy
openwakeword
pyaudio
onnxruntime

# Speech recognition
SpeechRecognition

# System automation
PyAutoGUI
PyGetWindow

# Brightness control (WMI / DDC-CI)
screen-brightness-control

# Text-to-Speech
edge-tts

# LLM client
httpx
